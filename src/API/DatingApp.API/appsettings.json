{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // To call an API
  "TokenKey": "[Copy the client secret added to the app from the Azure portal]"
}
